var nameSchema

module.exports = {
  handleSubmit: function (e){
  // e.preventDefault();
  // Axios
  //   .post(`/api/`, { headers: { 'Authorization': `Bearer ${Auth.getToken()}` } })
  // //   // .then(res => {
      console.log('handleSubmit!');
   //    nameSchema
   //      .create(req.body)
   //      .then(name => res.redirect(`/results`))
   //      .catch(err => {
   //     if(err.name === 'ValidationError') {
   //       return res.badRequest('/', err.toString());
   //     }
   //     next(err);
   // });
    //   (this.props.history.push(`/users/${Auth.getPayload().userId}`));
    // })
    // .catch(err => this.setState({ errors: err.response.data.errors }));
  }
};
